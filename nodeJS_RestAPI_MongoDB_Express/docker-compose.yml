version: '3'

services:
  node:
    image: node:16
    container_name: mongoAPI
    platform: linux/amd64
    working_dir: /app
    tty: true
    stdin_open: true
    ports:
      - 3000:3000
    volumes:
      - .:/app
    command: bash
